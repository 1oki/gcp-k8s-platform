apiVersion: v1
kind: Service
metadata:
  name: {{ include "spiral-app.fullname" . }}
  labels:
    {{- include "spiral-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "spiral-app.selectorLabels" . | nindent 4 }}
